diff --git a/cub/util_debug.cuh b/cub/util_debug.cuh
index 80b2bd33..0651c1df 100644
--- a/cub/util_debug.cuh
+++ b/cub/util_debug.cuh
@@ -300,12 +300,9 @@ inline __host__ __device__ void va_printf(char const *format,
 #endif
 }
 #ifndef __CUDA_ARCH__
-#define _CubLog(format, ...) CUB_NS_QUALIFIER::va_printf(format, __VA_ARGS__);
+#define _CubLog(format, ...) printf(format, __VA_ARGS__);
 #else
-#define _CubLog(format, ...)                                                   \
-  CUB_NS_QUALIFIER::va_printf("[block (%d,%d,%d), thread "                     \
-                              "(%d,%d,%d)]: " format,                          \
-                              __VA_ARGS__);
+#define _CubLog(format, ...) printf("[block (%d,%d,%d), thread (%d,%d,%d)]: " format, __VA_ARGS__);
 #endif
 #endif
 #endif
